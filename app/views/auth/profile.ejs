<%- include('../components/head', { title: title, isAuth: isAuth }); %>

<div class="container">
    <div class="card" style="width: 500px;">
        <div class="card-body">
            <h5 class="card-title mb-3">ID: <%= user.id %></h5>
            <h6 class="card-subtitle text-body-secondary">Login: <%= user.login %>,</h6>
            <p class="card-text">username: <%= user.displayName %></p>
        </div>
    </div>
</div>

<%- include('../components/foot'); %>
